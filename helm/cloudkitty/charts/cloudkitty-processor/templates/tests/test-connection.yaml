apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudkitty-processor.fullname" . }}-test-connection"
  labels:
{{ include "cloudkitty-processor.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cloudkitty-processor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
